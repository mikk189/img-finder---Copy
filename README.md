# Image Finder App — Parcel version (fixed, no Handlebars)

Parcel project for GoIT Homework 12, Handlebars removed; uses JS function for templates.

Setup & Run:
1. npm install
2. Set your Pixabay API key in src/js/fetchImages.js
3. npm start
